<h1>ログイン</h1>

<%= form_with(scope: :session, local: true) do |f| %>
  <table class="table">
    <tr>
      <%= f.label :email, "メールアドレス" %>
      <%= f.email_field :email %>
    </tr>
    <tr>
      <%= f.label :email, "メールアドレス" %>
      <%= f.email_field :email %>
    </tr>
  </table>
<% end %>